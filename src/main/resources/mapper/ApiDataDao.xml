<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gzkj.xsksh.dao.ApiDataDao">

    <resultMap type="com.gzkj.xsksh.entity.ApiData" id="ApiDataMap">
                    <result property="id" column="id" jdbcType="VARCHAR"/>
                    <result property="url" column="url" jdbcType="VARCHAR"/>
                    <result property="data" column="data" jdbcType="BLOB"/>
                    <result property="path" column="path" jdbcType="VARCHAR"/>
                    <result property="isDeleted" column="is_deleted" jdbcType="BOOLEAN"/>
                    <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
                    <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            </resultMap>

     <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into api_data(id, url, data, path, is_deleted, create_time, update_time)
        values (#{id}, #{url}, #{data}, #{path}, #{isDeleted}, #{createTime}, #{updateTime})
    </insert>
    <insert id="insertBatchs">
        insert into api_data(id, url, data, path, is_deleted, create_time, update_time)
        values
        <foreach collection="list" index="no" item="item" separator=",">
            (#{item.id}, #{item.url}, #{item.data}, #{item.path}, #{item.isDeleted}, #{item.createTime}, #{item.updateTime})
        
        </foreach>
    </insert>
    <!--通过主键删除-->
    <delete id="deleteById">
        delete from api_data where id = #{id}
    </delete>
    
    <!--通过主键集合删除-->
    <delete id="delete">
        delete from api_data where id in
        <foreach collection="list" item="id" index="no" open="("
                 separator="," close=")">
            #{id}
        </foreach>
    </delete>

        <!--通过主键修改数据-->
    <update id="update">
        update api_data
        <set>
        <if test="url != null and url != ''">
            url = #{url},
        </if>
        <if test="data != null">
            data = #{data},
        </if>
        <if test="path != null and path != ''">
            path = #{path},
        </if>
        <if test="isDeleted != null">
            is_deleted = #{isDeleted},
        </if>
        <if test="createTime != null">
            create_time = #{createTime},
        </if>
        <if test="updateTime != null">
            update_time = #{updateTime},
        </if>
                </set>
        where id = #{id}
    </update>

    <!--查询单个-->
    <select id="queryById" resultMap="ApiDataMap">
        select
          id, url, data, path, is_deleted, create_time, update_time          
        from api_data
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAll" resultMap="ApiDataMap">
        select
          id, url, data, path, is_deleted, create_time, update_time        from api_data
    </select>
    <select id="queryByUrl" resultType="java.lang.Integer">
        select count(1) from api_data
        where 1=1
        <if test="s!=null and s!=''">
            and url=#{s}
        </if>
    </select>
    <select id="queryLikeUrl" resultMap="ApiDataMap">
        select * from api_data
        where 1=1
        <if test="s!=null and s!=''">
            and url like '%${s}%'
        </if>
        limit 1
    </select>

</mapper>